name: PrometheusExporter
version: ${mod_version}
main: com.github.cpburnz.minecraft_prometheus_exporter.PrometheusExporterPlugin
api-version: '1.21'
description: ${mod_description}
author: ${mod_authors}
website: ${mod_url}

# This plugin only needs to run on the server
# No client-side installation required
load: STARTUP
